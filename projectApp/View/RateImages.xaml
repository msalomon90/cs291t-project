<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:swipecards="clr-namespace:SwipeCards.Controls;assembly=SwipeCards.Controls"
             mc:Ignorable="d"
             x:Class="projectApp.View.RateImages">
    <ContentPage.Content>
        <swipecards:CardStackView x:Name="CardStackView" ItemsSource="{Binding Cards}" Swiped="CardStackView_Swiped">
            <swipecards:CardStackView.ItemTemplate>
                <DataTemplate>
                    <!-- Image to display on card -->
                    <Image Source="{Binding Directory, Mode=TwoWay}" 
                           VerticalOptions="CenterAndExpand" 
                           HorizontalOptions="CenterAndExpand"/>
                </DataTemplate>
            </swipecards:CardStackView.ItemTemplate>
        </swipecards:CardStackView>
    </ContentPage.Content>
</ContentPage>